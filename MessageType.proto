syntax = "proto3";

package ipc;

message Header{
    fixed32 size = 1;
}

message Payload{
    fixed32 length = 1;
    bytes value = 2;
}

message Package{
    Header header = 1;
    Payload payload = 2;
}